# Copy this file to `.env.local` for local development.

# Backend API base URL
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Enables mock behavior in the frontend (if used by the app).
NEXT_PUBLIC_USE_MOCK=true
